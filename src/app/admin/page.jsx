"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Activity, Ban, CheckCircle2, ClipboardList, Eye, FileText, LogOut, ShieldCheck, Store, Trash2, Users, Wallet, XCircle } from "lucide-react";

const sections = [["Utilisateurs", Users, "Tous les comptes"], ["Restaurants", Store, "Statuts et abonnements"], ["Livreurs", Activity, "Validation des dossiers"], ["Commandes", ClipboardList, "Historique global"], ["Finances", Wallet, "Paiements et statistiques"], ["Modération", Ban, "Publications signalées"]];
const money = (value) => `${Number(value || 0).toLocaleString("fr-FR")} FCFA`;
const date = (value) => value ? new Date(value?.seconds ? value.seconds * 1000 : value).toLocaleDateString("fr-FR") : "Non défini";
const PAGE_SIZE = 20;

function RevenueCharts({ finance, selectedYear, setSelectedYear, selectedDay, setSelectedDay }) {
  const monthly = finance.monthlyRevenue || [];
  const hourly = finance.hourlyRevenue || [];
  const max = Math.max(...monthly.map((item) => item.revenue), 1);
  return <section className="revenue-charts"><div className="chart-toolbar"><label>Année<select value={selectedYear} onChange={(event) => setSelectedYear(Number(event.target.value))}>{[selectedYear - 2, selectedYear - 1, selectedYear, selectedYear + 1].map((year) => <option value={year} key={year}>{year}</option>)}</select></label><label>Jour précis<input type="date" value={selectedDay} onChange={(event) => setSelectedDay(event.target.value)} /></label>{selectedDay && <button type="button" onClick={() => setSelectedDay("")}>Réinitialiser</button>}</div><div className="chart-heading"><div><p className="eyebrow">ÉVOLUTION ANNUELLE</p><h3>Revenus plateforme en {selectedYear}</h3></div><strong>{money(monthly.reduce((total, item) => total + item.revenue, 0))}</strong></div><div className="revenue-chart">{monthly.map((item, index) => <div className="chart-column" key={item.month}><div className="chart-bar" style={{ height: `${Math.max(4, item.revenue / max * 100)}%` }} /><small>{["Jan", "Fév", "Mar", "Avr", "Mai", "Juin", "Juil", "Août", "Sep", "Oct", "Nov", "Déc"][index]}</small></div>)}</div>{selectedDay && <div className="day-chart"><div className="chart-heading"><h3>{new Date(`${selectedDay}T12:00:00`).toLocaleDateString("fr-FR", { dateStyle: "long" })}</h3><strong>{money(finance.dayRevenue)}</strong></div><div className="revenue-chart hourly-chart">{hourly.map((item) => <div className="chart-column" key={item.hour}><div className="chart-bar" style={{ height: `${Math.max(item.revenue ? 4 : 1, item.revenue / Math.max(...hourly.map((entry) => entry.revenue), 1) * 100)}%` }} /><small>{item.hour}h</small></div>)}</div></div>}</section>;
}

function SubscriptionControls({ accountType, accountId, currentPlan, currentStatus, action, safeAction }) {
  const [plan, setPlan] = useState(currentPlan || "Accès administrateur"); const [days, setDays] = useState(""); const [busy, setBusy] = useState(false);
  async function apply(mode) { if (mode === "activate" && (!plan.trim() || !Number(days))) return; setBusy(true); try { await safeAction(() => action("subscription", accountId, mode, "", { accountType, plan: plan.trim(), durationDays: Number(days) })); } finally { setBusy(false); } }
  return <div className="admin-subscription-controls"><strong>Abonnement</strong><span>{currentStatus || "inactif"}{currentPlan ? ` · ${currentPlan}` : ""}</span><div><input value={plan} onChange={(event) => setPlan(event.target.value)} placeholder="Nom du plan" disabled={busy}/><input type="number" min="1" max="3650" value={days} onChange={(event) => setDays(event.target.value)} placeholder="Jours" disabled={busy}/><button type="button" className="admin-action success" onClick={() => apply("activate")} disabled={busy || !days}>Activer</button><button type="button" className="admin-action danger" onClick={() => apply("deactivate")} disabled={busy || currentStatus !== "active"}>Désactiver</button></div></div>;
}
function UserSection({ users, search, setSearch, visibleCount, setVisibleCount, selectedUser, setSelectedUser, action, safeAction }) {
  const router = useRouter();
  return <div><div className="admin-section-toolbar"><label className="admin-search"><span>Rechercher un nom, e-mail ou téléphone</span><input value={search} onChange={(event) => { setSearch(event.target.value); setVisibleCount(PAGE_SIZE); }} placeholder="Ex. contact@miamgo.com" /></label><small>{users.length} résultat{users.length === 1 ? "" : "s"}</small></div>{users.length ? <div className="admin-real-list">{users.slice(0, visibleCount).map((user) => <article className="admin-list-row-clickable" key={user.uid} onClick={() => window.location.assign(`/admin/fiche/user/${user.uid}`)}><div><strong>{user.displayName || user.email || user.uid}</strong><small>{user.email} · {user.role || "client"} · Créé le {date(user.createdAt)}{user.phone ? ` · ${user.phone}` : ""}</small></div><div className="admin-actions"><button type="button" className="admin-detail-button" onClick={(event) => { event.stopPropagation(); window.location.assign(`/admin/fiche/user/${user.uid}`); }}>Voir la fiche</button><button type="button" className={user.disabled ? "admin-action success" : "admin-action danger"} onClick={(event) => { event.stopPropagation(); safeAction(() => action("user-suspension", user.uid, user.disabled ? "restore" : "suspend")); }}>{user.disabled ? "Réactiver" : "Suspendre"}</button></div></article>)}</div> : <div className="admin-table-placeholder"><Users size={30}/><h3>Aucun utilisateur trouvé</h3><p>Essayez avec un autre nom, e-mail ou numéro de téléphone.</p></div>}{visibleCount < users.length && <button className="admin-load-more" onClick={() => setVisibleCount((count) => count + PAGE_SIZE)}>Voir plus ({users.length - visibleCount} restant{users.length - visibleCount === 1 ? "" : "s"})</button>}{selectedUser && <aside className="admin-detail-panel"><button className="admin-detail-close" onClick={() => setSelectedUser(null)} aria-label="Fermer">×</button><p className="eyebrow">FICHE UTILISATEUR</p><h3>{selectedUser.displayName || selectedUser.email || selectedUser.uid}</h3><dl><dt>E-mail</dt><dd>{selectedUser.email || "Non renseigné"}</dd><dt>Téléphone</dt><dd>{selectedUser.phone || "Non renseigné"}</dd><dt>Rôle</dt><dd>{selectedUser.role || "client"}</dd><dt>Création</dt><dd>{date(selectedUser.createdAt)}</dd><dt>Statut</dt><dd>{selectedUser.disabled ? "Suspendu" : "Actif"}</dd></dl></aside>}</div>;
}

function RestaurantSection({ restaurants, search, setSearch, visibleCount, setVisibleCount, selectedRestaurant, setSelectedRestaurant, action, safeAction }) {
  const router = useRouter();
  return <div><div className="admin-section-toolbar"><label className="admin-search"><span>Rechercher une boutique</span><input value={search} onChange={(event) => { setSearch(event.target.value); setVisibleCount(PAGE_SIZE); }} placeholder="Ex. Chez Aïcha" /></label><small>{restaurants.length} résultat{restaurants.length === 1 ? "" : "s"}</small></div>{restaurants.length ? <div className="admin-real-list">{restaurants.slice(0, visibleCount).map((restaurant) => <article className="admin-list-row-clickable" key={restaurant.id} onClick={() => window.location.assign(`/admin/fiche/restaurant/${restaurant.id}`)}><div><strong>{restaurant.name || restaurant.id}</strong><small>Plan {restaurant.plan || "Non défini"} · {restaurant.subscriptionStatus || "inactif"} · Échéance {date(restaurant.subscriptionExpiresAt)}</small></div><div className="admin-actions"><button type="button" className="admin-detail-button" onClick={(event) => { event.stopPropagation(); window.location.assign(`/admin/fiche/restaurant/${restaurant.id}`); }}>Voir la fiche</button><button type="button" className={restaurant.status === "suspended" ? "admin-action success" : "admin-action danger"} onClick={(event) => { event.stopPropagation(); safeAction(() => action("restaurant-status", restaurant.id, restaurant.status === "suspended" ? "restore" : "suspend")); }}>{restaurant.status === "suspended" ? "Réactiver" : "Suspendre"}</button></div></article>)}</div> : <div className="admin-table-placeholder"><Store size={30}/><h3>Aucun restaurant trouvé</h3><p>Essayez avec un autre nom de boutique ou une autre ville.</p></div>}{visibleCount < restaurants.length && <button className="admin-load-more" onClick={() => setVisibleCount((count) => count + PAGE_SIZE)}>Voir plus ({restaurants.length - visibleCount} restant{restaurants.length - visibleCount === 1 ? "" : "s"})</button>}{selectedRestaurant && <aside className="admin-detail-panel"><button className="admin-detail-close" onClick={() => setSelectedRestaurant(null)} aria-label="Fermer">×</button><p className="eyebrow">FICHE RESTAURANT</p><h3>{selectedRestaurant.name || selectedRestaurant.id}</h3><dl><dt>Catégorie</dt><dd>{selectedRestaurant.category || "Non renseignée"}</dd><dt>Ville</dt><dd>{selectedRestaurant.city || "Non renseignée"}</dd><dt>Adresse</dt><dd>{selectedRestaurant.address || "Non renseignée"}</dd><dt>Plan</dt><dd>{selectedRestaurant.plan || "Non défini"}</dd><dt>Statut</dt><dd>{selectedRestaurant.status || selectedRestaurant.subscriptionStatus || "Inactif"}</dd><dt>Échéance</dt><dd>{date(selectedRestaurant.subscriptionExpiresAt)}</dd></dl><SubscriptionControls accountType="restaurant" accountId={selectedRestaurant.id} currentPlan={selectedRestaurant.plan || selectedRestaurant.subscriptionPlan} currentStatus={selectedRestaurant.subscriptionStatus} action={action} safeAction={safeAction}/></aside>}</div>;
}

function DriverSection({ drivers, search, setSearch, visibleCount, setVisibleCount, selectedDriver, setSelectedDriver, driverDecision, safeAction }) {
  const router = useRouter();
  return <div><div className="admin-section-toolbar"><label className="admin-search"><span>Rechercher un nom ou un téléphone</span><input value={search} onChange={(event) => { setSearch(event.target.value); setVisibleCount(PAGE_SIZE); }} placeholder="Ex. Jean ou +229..." /></label><small>{drivers.length} résultat{drivers.length === 1 ? "" : "s"}</small></div>{drivers.length ? <div className="admin-real-list">{drivers.slice(0, visibleCount).map((driver) => <article className="admin-list-row-clickable" key={driver.id} onClick={() => window.location.assign(`/admin/fiche/driver/${driver.id}`)}><div><strong>{driver.firstName} {driver.lastName}</strong><small>{driver.status || driver.verificationStatus || "pending"} · {driver.phone || "Téléphone non renseigné"} · {driver.city || "Ville non renseignée"}</small>{driver.identityDocumentUrl && <a className="admin-document" href={driver.identityDocumentUrl} target="_blank" rel="noreferrer" onClick={(event) => event.stopPropagation()}><Eye size={14}/>Voir la pièce d’identité</a>}</div><div className="admin-actions"><button type="button" className="admin-detail-button" onClick={(event) => { event.stopPropagation(); window.location.assign(`/admin/fiche/driver/${driver.id}`); }}>Voir la fiche</button>{driver.verificationStatus === "approved" ? <span className="tag success">Validé non abonné</span> : driver.verificationStatus === "rejected" ? <span className="tag danger">Rejeté</span> : <><button className="admin-action success" onClick={(event) => { event.stopPropagation(); safeAction(() => driverDecision(driver.id, "approve")); }}>Valider</button><button className="admin-action danger" onClick={(event) => { event.stopPropagation(); safeAction(() => driverDecision(driver.id, "reject")); }}>Rejeter</button></>}</div></article>)}</div> : <div className="admin-table-placeholder"><Activity size={30}/><h3>Aucun livreur trouvé</h3><p>Essayez avec un autre nom ou numéro de téléphone.</p></div>}{visibleCount < drivers.length && <button className="admin-load-more" onClick={() => setVisibleCount((count) => count + PAGE_SIZE)}>Voir plus ({drivers.length - visibleCount} restant{drivers.length - visibleCount === 1 ? "" : "s"})</button>}{selectedDriver && <aside className="admin-detail-panel"><button className="admin-detail-close" onClick={() => setSelectedDriver(null)} aria-label="Fermer">×</button><p className="eyebrow">FICHE LIVREUR</p><h3>{selectedDriver.firstName} {selectedDriver.lastName}</h3><dl><dt>Téléphone</dt><dd>{selectedDriver.phone || "Non renseigné"}</dd><dt>Ville</dt><dd>{selectedDriver.city || "Non renseignée"}</dd><dt>Validation</dt><dd>{selectedDriver.verificationStatus || "En attente"}</dd><dt>Abonnement</dt><dd>{selectedDriver.subscriptionStatus || "Non actif"}</dd><dt>Disponibilité</dt><dd>{selectedDriver.availabilityStatus || "Indisponible"}</dd></dl><SubscriptionControls accountType="driver" accountId={selectedDriver.id} currentPlan={selectedDriver.subscriptionPlan} currentStatus={selectedDriver.subscriptionStatus} action={action} safeAction={safeAction}/></aside>}</div>;
}

function OrderSection({ orders, search, setSearch, visibleCount, setVisibleCount, selectedOrder, setSelectedOrder }) {
  const router = useRouter();
  return <div><div className="admin-section-toolbar"><label className="admin-search"><span>Rechercher un numéro de commande</span><input value={search} onChange={(event) => { setSearch(event.target.value); setVisibleCount(PAGE_SIZE); }} placeholder="Ex. MG-XXXXX" /></label><small>{orders.length} résultat{orders.length === 1 ? "" : "s"}</small></div>{orders.length ? <div className="admin-real-list">{orders.slice(0, visibleCount).map((order) => <article className="admin-list-row-clickable" key={order.id} onClick={() => window.location.assign(`/admin/fiche/order/${order.id}`)}><div><strong>{order.serialNumber || order.id}</strong><small>{order.status || "pending"} · Paiement {order.paymentStatus || "pending"} · {money(order.total || order.amount)}</small></div><div className="admin-actions"><span>{date(order.createdAt)}</span><button type="button" className="admin-detail-button" onClick={(event) => { event.stopPropagation(); window.location.assign(`/admin/fiche/order/${order.id}`); }}>Voir le détail</button></div></article>)}</div> : <div className="admin-table-placeholder"><ClipboardList size={30}/><h3>Aucune commande trouvée</h3><p>Essayez avec un autre numéro de commande.</p></div>}{visibleCount < orders.length && <button className="admin-load-more" onClick={() => setVisibleCount((count) => count + PAGE_SIZE)}>Voir plus ({orders.length - visibleCount} restante{orders.length - visibleCount === 1 ? "" : "s"})</button>}{selectedOrder && <aside className="admin-detail-panel"><button className="admin-detail-close" onClick={() => setSelectedOrder(null)} aria-label="Fermer">×</button><p className="eyebrow">DÉTAIL DE LA COMMANDE</p><h3>{selectedOrder.serialNumber || selectedOrder.id}</h3><dl><dt>Statut</dt><dd>{selectedOrder.status || "En attente"}</dd><dt>Paiement</dt><dd>{selectedOrder.paymentStatus || "En attente"}</dd><dt>Client</dt><dd>{selectedOrder.clientName || selectedOrder.clientId || "Non renseigné"}</dd><dt>Restaurant</dt><dd>{selectedOrder.restaurantName || selectedOrder.restaurantId || "Non renseigné"}</dd><dt>Livreur</dt><dd>{selectedOrder.assignedCourierId || "Non assigné"}</dd><dt>Montant</dt><dd>{money(selectedOrder.total || selectedOrder.amount)}</dd><dt>Créée le</dt><dd>{date(selectedOrder.createdAt)}</dd></dl></aside>}</div>;
}

export default function AdminPage() {
  const router = useRouter();
  const [authenticated, setAuthenticated] = useState(false); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [section, setSection] = useState("Utilisateurs"); const [data, setData] = useState(null); const [loading, setLoading] = useState(false); const [period, setPeriod] = useState("month"); const [selectedYear, setSelectedYear] = useState(new Date().getFullYear()); const [selectedDay, setSelectedDay] = useState(""); const [search, setSearch] = useState(""); const [visibleCount, setVisibleCount] = useState(PAGE_SIZE); const [selectedUser, setSelectedUser] = useState(null); const [selectedRestaurant, setSelectedRestaurant] = useState(null); const [selectedDriver, setSelectedDriver] = useState(null); const [selectedOrder, setSelectedOrder] = useState(null);
  useEffect(() => { if (authenticated) load(period, selectedYear, selectedDay); }, [authenticated, period, selectedYear, selectedDay]);
  useEffect(() => { setSearch(""); setVisibleCount(PAGE_SIZE); setSelectedUser(null); setSelectedRestaurant(null); setSelectedDriver(null); setSelectedOrder(null); }, [section]);
  async function load(nextPeriod = period, nextYear = selectedYear, nextDay = selectedDay) { setLoading(true); setError(""); try { const query = new URLSearchParams({ period: nextPeriod, year: String(nextYear) }); if (nextDay) query.set("day", nextDay); const response = await fetch(`/api/admin/overview?${query}`, { cache: "no-store" }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "Erreur admin."); setData(payload); } catch (cause) { setError(cause.message); } finally { setLoading(false); } }
  async function login(event) { event.preventDefault(); setError(""); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) }); if (!response.ok) { setError("Mot de passe administrateur invalide."); return; } setAuthenticated(true); load(); }
  async function action(actionName, id, mode, reason = "", extra = {}) { const response = await fetch("/api/admin/action", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: actionName, id, mode, reason, ...extra }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "Action impossible."); await load(); }
  async function driverDecision(id, decision) { const reason = decision === "reject" ? window.prompt("Motif du rejet", "Pièce d’identité illisible ou dossier incomplet") || "Dossier rejeté." : ""; const response = await fetch(`/api/admin/drivers/${id}/decision`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ decision, reason }) }); const responseText = await response.text(); const payload = responseText ? JSON.parse(responseText) : {}; if (!response.ok) throw new Error(payload.error || `Décision impossible (HTTP ${response.status}).`); await load(); }
  async function safeAction(fn) { try { await fn(); } catch (cause) { setError(cause.message); } }
  if (!authenticated) return <main className="admin-login"><section><ShieldCheck size={35}/><p className="eyebrow">ZONE PRIVÉE MIAMGO</p><h1>Administration</h1><p>Console réservée à l’équipe créatrice.</p><form onSubmit={login}><input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Mot de passe administrateur" required/><button>Accéder</button></form>{error && <small>{error}</small>}</section></main>;
  const count = data?.counts || {}; const finance = data?.finance || {}; const list = section === "Utilisateurs" ? data?.users : section === "Restaurants" ? data?.restaurants : section === "Livreurs" ? data?.drivers : section === "Commandes" ? data?.orders : section === "Modération" ? data?.posts : null; const searchValue = search.trim().toLocaleLowerCase(); const users = (data?.users || []).filter((user) => !searchValue || [user.displayName, user.email, user.phone, user.uid, user.role].some((value) => String(value || "").toLocaleLowerCase().includes(searchValue))); const restaurants = (data?.restaurants || []).filter((restaurant) => !searchValue || [restaurant.name, restaurant.slug, restaurant.category, restaurant.city].some((value) => String(value || "").toLocaleLowerCase().includes(searchValue))); const drivers = (data?.drivers || []).filter((driver) => !searchValue || [`${driver.firstName || ""} ${driver.lastName || ""}`, driver.phone, driver.city, driver.status, driver.verificationStatus].some((value) => String(value || "").toLocaleLowerCase().includes(searchValue))); const orders = (data?.orders || []).filter((order) => !searchValue || [order.serialNumber, order.id, order.status, order.paymentStatus].some((value) => String(value || "").toLocaleLowerCase().includes(searchValue))); const paidSubscriptions = (data?.subscriptions || []).filter((item) => ["paid", "active", "completed"].includes(item.status || item.paymentStatus));
  return <main className="admin-page"><header className="admin-header"><div><p className="eyebrow">CONSOLE MIAMGO</p><h1>Administration</h1><p>Contrôle opérationnel et financier en temps réel.</p></div><div className="admin-header-actions"><button className="admin-refresh" onClick={load}>{loading ? "Actualisation..." : "Actualiser"}</button><button className="admin-logout" onClick={async () => { await fetch("/api/admin/logout", { method: "POST" }); setAuthenticated(false); setData(null); }}><LogOut size={16}/>Sortir</button></div></header>{error && <div className="admin-error-box"><XCircle size={17}/>{error}</div>}<section className="admin-metrics"><article><Users size={20}/><span>Utilisateurs</span><strong>{count.users || 0}</strong></article><article><Store size={20}/><span>Restaurants</span><strong>{count.restaurants || 0}</strong></article><article><Activity size={20}/><span>Livreurs</span><strong>{count.drivers || 0}</strong></article><article><Wallet size={20}/><span>CA plateforme</span><strong>{money(finance.totalRevenue)}</strong></article></section><div className="admin-layout"><nav className="admin-menu">{sections.map(([label, Icon, description]) => <button className={section === label ? "active" : ""} onClick={() => setSection(label)} key={label}><Icon size={18}/><span><strong>{label}</strong><small>{description}</small></span></button>)}</nav><section className="admin-workspace"><div className="admin-title"><div><p className="eyebrow">{section.toUpperCase()}</p><h2>{section}</h2></div><span className="admin-status"><CheckCircle2 size={14}/>Données Firestore</span></div>
    {section === "Utilisateurs" && <UserSection users={users} search={search} setSearch={setSearch} visibleCount={visibleCount} setVisibleCount={setVisibleCount} selectedUser={selectedUser} setSelectedUser={setSelectedUser} action={action} safeAction={safeAction} />}
    {section === "Restaurants" && <RestaurantSection restaurants={restaurants} search={search} setSearch={setSearch} visibleCount={visibleCount} setVisibleCount={setVisibleCount} selectedRestaurant={selectedRestaurant} setSelectedRestaurant={setSelectedRestaurant} action={action} safeAction={safeAction} />}
    {section === "Livreurs" && <DriverSection drivers={drivers} search={search} setSearch={setSearch} visibleCount={visibleCount} setVisibleCount={setVisibleCount} selectedDriver={selectedDriver} setSelectedDriver={setSelectedDriver} driverDecision={driverDecision} safeAction={safeAction} />}
    {section === "Commandes" && <OrderSection orders={orders} search={search} setSearch={setSearch} visibleCount={visibleCount} setVisibleCount={setVisibleCount} selectedOrder={selectedOrder} setSelectedOrder={setSelectedOrder} />}
    {section === "Finances" && <><div className="finance-period"><label>Période<select value={period} onChange={(event) => setPeriod(event.target.value)}><option value="day">Jour</option><option value="month">Mois</option><option value="year">Année</option></select></label></div><div className="finance-grid"><article><Wallet size={20}/><span>Abonnements reçus</span><strong>{money(finance.subscriptionRevenue)}</strong><small>{finance.paidSubscriptions || 0} paiements confirmés</small></article><article><ClipboardList size={20}/><span>Frais de commande</span><strong>{money(finance.orderFees)}</strong><small>{finance.paidOrders || 0} commandes payées</small></article><article><CheckCircle2 size={20}/><span>Total plateforme</span><strong>{money(finance.totalRevenue)}</strong><small>Calculé depuis Firestore</small></article></div><RevenueCharts finance={finance} selectedYear={selectedYear} setSelectedYear={setSelectedYear} selectedDay={selectedDay} setSelectedDay={setSelectedDay}/><div className="admin-real-list">{paidSubscriptions.map((payment) => <article key={payment.id}><div><strong>{payment.restaurantId || payment.userId || "Paiement"}</strong><small>{payment.plan || payment.type || "Abonnement"} · {money(payment.amount || payment.price)} · {date(payment.createdAt)}</small></div></article>)}</div></>}
    {section === "Modération" && <div className="admin-real-list">{(list || []).filter((post) => post.moderationStatus !== "removed").map((post) => <article key={post.id}><div><strong>{post.restaurantName || post.restaurantId || "Publication"}</strong><small>{post.text || post.dish || "Sans texte"} · {date(post.createdAt)}</small>{post.mediaUrl && <a className="admin-document" href={post.mediaUrl} target="_blank" rel="noreferrer"><FileText size={14}/>Voir le média</a>}</div><button className="admin-action danger" onClick={() => safeAction(() => action("remove-post", post.id, "remove", "Contenu retiré par la modération."))}><Trash2 size={14}/>Retirer</button></article>)}</div>}
    {!list && section !== "Finances" && <div className="admin-table-placeholder"><ClipboardList size={30}/><h3>Aucune donnée</h3><p>Les données réelles apparaîtront ici dès leur création.</p></div>}
  </section></div></main>;
}
