"use client";

import { useEffect, useRef, useState } from "react";
import Link from "next/link";
import { ArrowLeft, Check, CheckCircle2, MapPin, Navigation, PackageCheck, Play, Store } from "lucide-react";
import { onAuthStateChanged } from "firebase/auth";
import PlatformShell from "../../../components/PlatformShell";
import DeliveryTrackingMap from "../../../components/DeliveryTrackingMap";
import { auth } from "../../../lib/firebase";
import { getActiveDriverOrder } from "../../../lib/firestore";
import { startDriverLocationTracking } from "../../../lib/realtimeLocation";

export default function DriverActiveCourse() {
  const [user, setUser] = useState(null); const [order, setOrder] = useState(null); const [status, setStatus] = useState(""); const [loading, setLoading] = useState(false); const trackingStop = useRef(null);
  useEffect(() => onAuthStateChanged(auth, async (session) => { setUser(session); if (session) setOrder(await getActiveDriverOrder(session.uid).catch(() => null)); }), []);
  useEffect(() => () => trackingStop.current?.(), []);
  async function accept() { if (!user || !order) return; setLoading(true); try { const token = await user.getIdToken(); const response = await fetch("/api/delivery/accept", { method: "POST", headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" }, body: JSON.stringify({ orderId: order.id }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error); setOrder(await getActiveDriverOrder(user.uid)); } catch (error) { setStatus(error.message || "Impossible d’accepter cette course."); } finally { setLoading(false); } }
  async function startTrip() { if (!user || !order) return; setLoading(true); try { const token = await user.getIdToken(); const response = await fetch("/api/delivery/start", { method: "POST", headers: { Authorization: `Bearer ${token}`, "Content-Type": "application/json" }, body: JSON.stringify({ orderId: order.id }) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error); setOrder({ ...order, deliveryStatus: "en_cours_livraison", status: "out_for_delivery", trackingActive: true }); trackingStop.current?.(); trackingStop.current = startDriverLocationTracking(order.id, user.uid); } catch (error) { setStatus(error.message || "Impossible de démarrer la course."); } finally { setLoading(false); } }
  const items = Array.isArray(order?.items) ? order.items : [];
  return <PlatformShell><main className="content-wrap driver-dashboard"><Link className="back-link" href="/espace-livreur"><ArrowLeft size={17}/>Retour au tableau de bord</Link><p className="eyebrow">COURSE EN COURS</p><h1>{order ? order.serialNumber || "Course assignée" : "Aucune course active"}</h1>{status && <p className="settings-notice">{status}</p>}{order ? <><section className="active-delivery-summary"><div><Store size={20}/><div><span>Restaurant</span><strong>{order.restaurantName || order.restaurantId || "Restaurant Miamgo"}</strong><small>{order.restaurantAddress || "Adresse du restaurant non renseignée"}</small></div></div><div><MapPin size={20}/><div><span>Destinataire</span><strong>{order.customerName || "Client"}</strong><small>{order.deliveryAddress || order.address || "Adresse de livraison non renseignée"}</small></div></div></section><section className="active-delivery-items"><h2><PackageCheck size={20}/>Contenu de la commande</h2>{items.length ? items.map((item, index) => <div key={`${item.id || item.name}-${index}`}><span>{item.quantity || 1} × {item.name || "Article"}</span><strong>{item.price ? `${item.price} FCFA` : ""}</strong></div>) : <p>Le détail des articles n’est pas renseigné.</p>}</section>{order.deliveryStatus === "proposee" ? <button className="driver-start-trip" onClick={accept} disabled={loading}><Check size={17}/>{loading ? "Confirmation..." : "Accepter la course"}</button> : order.deliveryStatus === "en_cours_livraison" ? <><DeliveryTrackingMap orderId={order.id} driverId={user.uid} title="Votre position est partagée"/><p className="tracking-instruction"><Navigation size={16}/>Le suivi reste actif jusqu’à la validation du QR de livraison.</p></> : <button className="driver-start-trip" onClick={startTrip} disabled={loading}><Play size={17}/>{loading ? "Démarrage..." : "Démarrer le trajet"}</button>}<Link className="driver-validate-link" href={`/espace-livreur/scanner?order=${order.id}`}><CheckCircle2 size={16}/>Valider la livraison par QR</Link></> : <section className="driver-empty"><PackageCheck size={32}/><h2>Aucune course active</h2><p>Les commandes proposées par les restaurants apparaîtront ici.</p></section>}</main></PlatformShell>;
}
