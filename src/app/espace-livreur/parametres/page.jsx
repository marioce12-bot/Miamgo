"use client";
import Link from "next/link";
import { ArrowLeft, Save } from "lucide-react";
import PlatformShell from "../../../components/PlatformShell";
import { usePreferences } from "../../../components/PreferencesProvider";
export default function DriverSettings(){const preferences=usePreferences();return <PlatformShell><main className="content-wrap driver-dashboard"><Link className="back-link" href="/espace-livreur/profil"><ArrowLeft size={17}/>Retour au profil</Link><p className="eyebrow">PARAMÈTRES DU COMPTE</p><h1>{preferences?.t("settings")||"Paramètres"}</h1><form className="driver-settings-form" onSubmit={(event)=>event.preventDefault()}><label>{preferences?.t("language")||"Langue"}<select value={preferences?.language||"fr"} onChange={(event)=>preferences?.setLanguage(event.target.value)}><option value="fr">Français</option><option value="en">English</option></select></label><label>{preferences?.t("theme")||"Thème"}<select value={preferences?.theme||"light"} onChange={(event)=>preferences?.setTheme(event.target.value)}><option value="light">Clair</option><option value="dark">Sombre</option></select></label><button><Save size={16}/>{preferences?.t("save")||"Enregistrer"}</button></form></main></PlatformShell>}
