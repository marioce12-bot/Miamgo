"use client";
import { useEffect, useState } from "react";
import { MapPin, Store } from "lucide-react";
import PlatformShell from "../../../components/PlatformShell";
import { getRestaurantBySlug, getRestaurantPosts } from "../../../lib/firestore";
export default function PublicRestaurantProfile() { const [restaurant,setRestaurant]=useState(null); const [posts,setPosts]=useState([]); const slug=typeof window!=="undefined"?window.location.pathname.split("/").pop():""; useEffect(()=>{if(!slug)return;getRestaurantBySlug(slug).then(async(owned)=>{setRestaurant(owned);if(owned)setPosts(await getRestaurantPosts(owned.id).catch(()=>[]));}).catch(()=>{});},[slug]); if(!restaurant)return <PlatformShell><main className="content-wrap"><h1>Restaurant introuvable</h1></main></PlatformShell>; return <PlatformShell><main className="restaurant-profile-page public-restaurant-profile"><section className="restaurant-public-profile"><div className="restaurant-profile-cover" style={restaurant.coverURL?{backgroundImage:`url(${restaurant.coverURL})`}:{}}/><div className="restaurant-profile-body"><div className="restaurant-profile-avatar" style={restaurant.photoURL?{backgroundImage:`url(${restaurant.photoURL})`}:{}}>{!restaurant.photoURL&&<Store size={22}/>}</div><div className="restaurant-profile-title"><p className="eyebrow">RESTAURANT</p><h1>{restaurant.name}</h1><p><MapPin size={13}/>{restaurant.city||"Ville non renseignée"}</p></div></div></section><section className="restaurant-post-history"><p className="eyebrow">PUBLICATIONS</p><h2>Les dernières publications</h2><div className="restaurant-profile-posts">{posts.map((post)=><article key={post.id}><div className="restaurant-profile-post-media">{post.mediaType==="video"?<video src={post.mediaUrl} controls/>:<img src={post.mediaUrl} alt={post.dish||"Publication"}/>}</div><div className="restaurant-profile-post-content"><p>{post.text}</p><strong>{post.dish} {post.price?`· ${post.price} FCFA`:""}</strong></div></article>)}</div></section></main></PlatformShell>; }