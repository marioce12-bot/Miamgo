"use client";

import { useState } from "react";
import { Eye, EyeOff, Pencil, Plus, Trash2 } from "lucide-react";
import PlatformShell from "../../../components/PlatformShell";

const initialItems = [{ name: "Riz gras au poulet fumé", price: "2 500 FCFA", category: "Plats du jour", available: true }, { name: "Poulet braisé entier", price: "4 500 FCFA", category: "Grillades", available: true }, { name: "Jus de bissap maison", price: "500 FCFA", category: "Boissons", available: false }];
export default function RestaurantMenu() { const [items, setItems] = useState(initialItems); const [adding, setAdding] = useState(false); return <PlatformShell><main className="content-wrap restaurant-menu-page"><div className="restaurant-orders-heading"><div><p className="eyebrow">CATALOGUE RESTAURANT</p><h1>Votre menu</h1><p>Gérez vos catégories, prix, disponibilités et plats du jour.</p></div><button onClick={() => setAdding(true)}><Plus size={18} />Ajouter un plat</button></div><div className="menu-category-pills"><button className="active">Tous les plats</button><button>Plats du jour</button><button>Grillades</button><button>Boissons</button><button>Promotions</button></div><section className="restaurant-menu-list">{items.map((item, index) => <article key={item.name}><div className="menu-item-photo">🍲</div><div><span>{item.category}</span><h2>{item.name}</h2><strong>{item.price}</strong></div><button onClick={() => setItems((current) => current.map((entry, i) => i === index ? { ...entry, available: !entry.available } : entry))}>{item.available ? <Eye size={17} /> : <EyeOff size={17} />}{item.available ? "Disponible" : "Masqué"}</button><button><Pencil size={17} /></button><button onClick={() => setItems((current) => current.filter((_, i) => i !== index))}><Trash2 size={17} /></button></article>)}</section>{adding && <div className="modal-backdrop"><section className="identity-modal"><h2>Ajouter un plat</h2><form onSubmit={(event) => { event.preventDefault(); const data = new FormData(event.currentTarget); setItems((current) => [...current, { name: data.get("name"), price: data.get("price"), category: data.get("category"), available: true }]); setAdding(false); }}><label>Nom<input name="name" required /></label><label>Prix<input name="price" required placeholder="2 500 FCFA" /></label><label>Catégorie<input name="category" required placeholder="Plats du jour" /></label><label>Photo Imgbb<input name="image" placeholder="https://i.ibb.co/..." /></label><button>Ajouter au menu</button></form></section></div>}</main></PlatformShell> }
