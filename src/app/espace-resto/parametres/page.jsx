"use client";
import { useState } from "react";
import { Save, Users } from "lucide-react";
import PlatformShell from "../../../components/PlatformShell";
import { usePreferences } from "../../../components/PreferencesProvider";
export default function RestaurantSettings(){const[notice,setNotice]=useState("");const preferences=usePreferences();return <PlatformShell><main className="content-wrap restaurant-settings-page"><p className="eyebrow">{preferences?.t("settings")||"PARAMÈTRES PROFESSIONNELS"}</p><h1>{preferences?.t("settings")||"Informations du responsable"}</h1><form onSubmit={event=>{event.preventDefault();setNotice(preferences?.t("save")||"Paramètres enregistrés.");}}><label>Nom complet<input placeholder="Nom du responsable"/></label><label>E-mail professionnel<input type="email" placeholder="contact@restaurant.com"/></label><label>Numéro WhatsApp<input placeholder="+229 00 00 00 00"/></label><label>{preferences?.t("language")||"Langue"}<select value={preferences?.language||"fr"} onChange={event=>preferences?.setLanguage(event.target.value)}><option value="fr">Français</option><option value="en">English</option></select></label><label>{preferences?.t("theme")||"Thème"}<select value={preferences?.theme||"light"} onChange={event=>preferences?.setTheme(event.target.value)}><option value="light">Clair</option><option value="dark">Sombre</option></select></label><button><Save size={16}/>{preferences?.t("save")||"Enregistrer"}</button></form>{notice&&<p className="settings-notice">{notice}</p>}<section className="employees-section"><h2><Users size={18}/>Employés</h2><p>Gérez les employés ayant le droit de préparer et valider les retraits.</p><button>Inviter un employé</button></section></main></PlatformShell>}
