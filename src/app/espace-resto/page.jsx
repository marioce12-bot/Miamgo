"use client";

import { useState } from "react";
import Link from "next/link";
import { ArrowUpRight, BarChart3, BellRing, Check, ChevronRight, ClipboardList, Clock3, Plus, Store, Wallet } from "lucide-react";
import PlatformShell from "../../components/PlatformShell";

const orders = [["#MG-0842", "Marie A.", "Riz gras au poulet", "12:45", "En préparation", "orange"], ["#MG-0841", "Paul K.", "2 plats · Retrait", "12:38", "Prête", "green"], ["#MG-0840", "Jean D.", "Poisson braisé", "12:21", "Livraison", "blue"]];

export default function RestaurantDashboard() {
  const [notice, setNotice] = useState(""); const [showAll, setShowAll] = useState(false);
  const notify = (message) => { setNotice(message); setTimeout(() => setNotice(""), 2600); };
  return <PlatformShell><main className="dashboard-page"><aside className="dashboard-side"><div className="dashboard-identity"><span>CA</span><div><strong>Chez Aïcha</strong><small>Boutique ouverte</small></div></div><nav><a className="active"><BarChart3 size={18} />Vue d&apos;ensemble</a><a onClick={() => setShowAll(true)}><ClipboardList size={18} />Commandes <b>3</b></a><Link href="/restaurant/chez-aicha"><Store size={18} />Mon menu</Link><a onClick={() => notify("Le créateur de promotion est ouvert dans votre espace restaurant.")}><BellRing size={18} />Promotions</a><Link href="/livreurs"><Clock3 size={18} />Livreurs</Link><a onClick={() => notify("Les données financières sont disponibles dans vos statistiques mensuelles.")}><Wallet size={18} />Finances</a></nav><Link className="view-store" href="/restaurant/chez-aicha">Voir ma boutique <ArrowUpRight size={15} /></Link></aside><section className="dashboard-content"><div className="dashboard-heading"><div><p className="eyebrow">MARDI 7 AOÛT</p><h1>Bonjour, Aïcha.</h1><p>Voici ce qui se passe dans votre restaurant aujourd&apos;hui.</p></div><button onClick={() => notify("Le formulaire d'ajout de plat est prêt à être relié à votre menu Firestore.")}><Plus size={17} />Ajouter un plat</button></div>{notice && <p className="dashboard-notice">{notice}</p>}<div className="metric-grid"><article><span className="metric-icon money"><Wallet size={20} /></span><p>Ventes aujourd&apos;hui</p><strong>48 500 <small>FCFA</small></strong><em>+18% vs hier</em></article><article><span className="metric-icon orders"><ClipboardList size={20} /></span><p>Commandes</p><strong>24</strong><em>6 en attente</em></article><article><span className="metric-icon customers"><BarChart3 size={20} /></span><p>Panier moyen</p><strong>2 020 <small>FCFA</small></strong><em>+4% vs hier</em></article></div><section className="dashboard-orders"><div className="section-heading"><div><p className="eyebrow">À TRAITER</p><h2>Commandes en cours</h2></div><button onClick={() => setShowAll(!showAll)}>{showAll ? "Réduire" : "Voir toutes"} <ChevronRight size={16} /></button></div><div className="orders-table"><div className="table-head"><span>Commande</span><span>Client</span><span>Heure</span><span>Statut</span><span /></div>{orders.slice(0, showAll ? orders.length : 2).map(([number, client, meal, time, status, color]) => <article key={number}><div><strong>{number}</strong><small>{meal}</small></div><span>{client}</span><span>{time}</span><b className={color}>{status}</b><button onClick={() => notify(`Commande ${number} sélectionnée.`)}><ChevronRight size={17} /></button></article>)}</div></section><section className="quick-actions"><h2>Accès rapide</h2><button onClick={() => notify("Création du plat du jour ouverte.")}><Plus size={18} />Créer le plat du jour</button><button onClick={() => notify("Planification de promotion ouverte.")}><BellRing size={18} />Lancer une promotion</button><button onClick={() => notify("Disponibilités enregistrées.")}><Check size={18} />Gérer les disponibilités</button></section></section></main></PlatformShell>;
}
