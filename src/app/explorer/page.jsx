"use client";
import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { MapPin, Search, ShoppingBag, Star, X } from "lucide-react";
import { onAuthStateChanged } from "firebase/auth";
import PlatformShell from "../../components/PlatformShell";
import { auth } from "../../lib/firebase";
import { addCartItem, getExplorerItems } from "../../lib/firestore";
const categories = ["Tous", "Plats du jour", "Grillades", "Boissons", "Cocktails", "Promotions"];
export default function ExplorerPage(){const [items,setItems]=useState([]);const [query,setQuery]=useState("");const [category,setCategory]=useState("Tous");const [selected,setSelected]=useState(null);const [user,setUser]=useState(null);const [message,setMessage]=useState("");useEffect(()=>{onAuthStateChanged(auth,setUser);getExplorerItems().then(setItems).catch(()=>setItems([]));},[]);const filtered=useMemo(()=>items.filter((item)=>{const text=`${item.name||""} ${item.restaurantName||""} ${item.category||""}`.toLowerCase();return(category==="Tous"||item.category===category)&&text.includes(query.toLowerCase());}),[items,query,category]);async function add(item){if(!user){setMessage("Connectez-vous pour ajouter un plat au panier.");return;}try{await addCartItem(user.uid,{id:item.id,dish:item.name,price:Number(item.price||0),restaurant:item.restaurantId});setMessage(`${item.name} a été ajouté au panier.`);}catch(error){setMessage(error.message||"Impossible d’ajouter ce plat.");}}return <PlatformShell active="Explorer"><main className="content-wrap explorer-pinterest"><section className="explorer-hero"><p className="eyebrow">EXPLORER MIAMGO</p><h1>Les plats qui donnent envie.</h1><p>Découvrez les menus réels des restaurants actifs près de vous.</p><label className="large-search"><Search size={20}/><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Plat, restaurant, catégorie..."/></label></section><div className="explorer-category-row">{categories.map((item)=><button className={category===item?"active":""} onClick={()=>setCategory(item)} key={item}>{item}</button>)}</div>{message&&<p className="settings-notice">{message}</p>}{filtered.length?<section className="pinterest-grid">{filtered.map((item)=><button className="pinterest-card" onClick={()=>setSelected(item)} key={`${item.restaurantId}-${item.id}`}><div className="pinterest-image">{item.imageUrl?<img src={item.imageUrl} alt={item.name}/>:<div className="pinterest-placeholder">{(item.name||"M").slice(0,1)}</div>}<span>{item.category}</span></div><div className="pinterest-card-body"><strong>{item.name}</strong><small>{item.restaurantName} · {item.city}</small><b>{item.price} FCFA</b></div></button>)}</section>:<div className="empty-state"><Search size={34}/><h2>Aucun plat trouvé</h2><p>Les menus réels des restaurants actifs apparaîtront ici.</p></div>}{selected&&<div className="modal-backdrop" onClick={()=>setSelected(null)}><section className="explorer-detail-modal" onClick={(event)=>event.stopPropagation()}><button className="explorer-detail-close" onClick={()=>setSelected(null)}><X size={18}/></button>{selected.imageUrl?<img src={selected.imageUrl} alt={selected.name}/>:<div className="pinterest-placeholder large">{(selected.name||"M").slice(0,1)}</div>}<p className="eyebrow">{selected.restaurantName}</p><h2>{selected.name}</h2><p>{selected.category} · {selected.city}</p><strong className="explorer-detail-price">{selected.price} FCFA</strong><div className="explorer-detail-actions"><button onClick={()=>add(selected)}><ShoppingBag size={16}/>Ajouter au panier</button><Link href={`/restaurant/${selected.restaurantSlug}`}>Voir le restaurant</Link></div></section></div>}</main></PlatformShell>;
}